from main import System, save_data, fetch_data, pi, make_animation, make_plot_doublepend, make_plot_singlepend, make_phase_space, make_plot_error
from pendulum import Pendulum

# double_pendulum = System(1, 1, 1, 1, pi/2, 0, pi/2, 0, 1000, 20)  Example code showing how to use the interface
# pend_data_1 = double_pendulum.make_data(method='Radau')
# pend_data_2 = double_pendulum.make_data(method='RK45')

# make_plot_doublepend(pend_data_1)
# make_animation(double_pendulum, pend_data_1)
# make_plot_error(pend_data_1, pend_data_2)

# simple_pendulum = Pendulum.init_simple_pendulum(1, 1, pi/2, 0, 1000, 20)
# pend_data_simple = simple_pendulum.make_data()

# make_plot_singlepend(pend_data_simple)

# make_phase_space(1, 1, 1, 1, 100, 10, 10)














